extends layout

block content
  h3.
    Students
  ul
    each student, i in studentlist
      li#student_list_item
        a(href='#') #{student.username} has PASSWORD: #{student.password} and EMAIL:  #{student.email}